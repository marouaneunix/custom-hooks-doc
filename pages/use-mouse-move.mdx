# use-mouse-move hook

## Get Started

import { Callout } from 'nextra-theme-docs'

<Callout type="warning" emoji="⚠️">
  Docs is under construction.
</Callout>

## Description

This hook allows you to get the mouse position on the screen.

## install

You can install the package via npm or yarn:

import { Tab, Tabs } from 'nextra-theme-docs'

<Tabs items={['npm', 'yarn']}>
  <Tab>
    ```shell copy
      npm install -d @melmerrouni/custom-hooks
      ```
  </Tab>
  <Tab>
    ```bash copy
     yarn add @melmerrouni/custom-hooks -D
    ```
  </Tab>
</Tabs>

## Example

You just need to import `useMouseMove` from `@melmerrouni/custom-hooks`:

```tsx filename="demo.tsx" copy

import {useMouseMove} from '@melmerrouni/custom-hooks';

const Target = () => {
  const {position, ref} = useMouseMove();
  
  return (
    <div data-testid="target" ref={ref}>
      {position && `${position.clientX}${position.clientY}`}
    </div>
  )
};
```

## Demo 

import UseMouseMoveHookDemo from '../components/UseMouseMoveHookDemo'
 
<UseMouseMoveHookDemo/>